'use strict';

describe('myApp.rawData module', function() {

  beforeEach(module('myApp.rawData'));

  describe('rawData controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var rawDataCtrl = $controller('RawDataCtrl');
      expect(rawDataCtrl).toBeDefined();
    }));

  });
});