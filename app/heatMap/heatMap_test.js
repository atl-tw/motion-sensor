'use strict';

describe('myApp.heatMap module', function() {

  beforeEach(module('myApp.heatMap'));

  describe('heatMap controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var heatMapCtrl = $controller('HeatMapCtrl');
      expect(heatMapCtrl).toBeDefined();
    }));

  });
});